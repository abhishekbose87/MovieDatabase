%h2 Edit Movie details
= flash.now[:notice] unless flash.now[:notice].blank?
= form_for @movie do |f|
  Title: #{f.text_field :title}
  Description: #{f.text_area :description}
  Cast: #{f.text_area :cast}
  Language: #{f.text_field :language}
  %h3
    = f.submit "Update movie"
